# Environment Variables Example File
# Rename this file to .env and update the values according to your deployment configuration.

# Base URL of the application:
# - For local development, typically you might use http://localhost:3000 or another port if 3000 is in use.
# - For production, replace with the actual URL where your application is hosted, e.g., https://your-application-url.com.
BASE_URL=http://localhost:3000

# Administrator password for accessing the admin panel
# Ensure to use a secure password in production
ADMIN_PASSWORD=your_admin_password

# Cloudinary API Key for accessing Cloudinary services
# Replace 'your_cloudinary_key' with the actual API key provided by Cloudinary
CLOUDINARY_KEY=your_cloudinary_key

# Cloudinary API Secret for accessing Cloudinary services
# Replace 'your_cloudinary_secret' with the actual secret provided by Cloudinary
CLOUDINARY_SECRET=your_cloudinary_secret

# Instructions:
# 1. Copy this file and rename it to '.env'.
# 2. Replace placeholder values with your actual sensitive data for local and production environments.
# 3. Never commit your '.env' file with real credentials to version control systems. Exclude it using .gitignore.
